server:
  port: 8086

spring:
  application:
    name: msgpack-poc

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    org.techishthoughts.msgpack: DEBUG
    org.springframework.web: INFO
